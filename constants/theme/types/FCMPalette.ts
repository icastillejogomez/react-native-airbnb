export type FCMPalette = {
  color: {
    primary: string
    secondary: string
    tertiary: string
    error: string
    warning: string
    success: string
    info: string
  }
  text: {
    default: string
    neutral: string
  }
  background: {
    primary: string
    header: string
  }
  header: {
    exploreHeader: {
      border: string
      indicator: string
    }
  }
  tabBar: {
    background: string
    border: string
    label: string
    icon: string
    labelTint: string
    iconTint: string
  }
}
