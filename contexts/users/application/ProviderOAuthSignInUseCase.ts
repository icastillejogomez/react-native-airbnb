import { OAuthProvider } from '@/types'

export class ProviderOAuthSignInUseCase {
  public async execute(provider: OAuthProvider): Promise<void> {
    throw new Error('Method not implemented.')
  }
}
